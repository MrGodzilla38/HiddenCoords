org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Build against the oldest supported release. This ensures the remapped jar
# cannot accidentally call a client API introduced in a later 1.21 patch.
minecraft_version=1.21
yarn_mappings=1.21+build.9
loader_version=0.16.14
fabric_version=0.100.8+1.21

mod_version=1.0.0
maven_group=dev.hiddencoords
archives_base_name=hidden-coords
